{
  "name": "@modular-everything/root",
  "private": true,
  "scripts": {
    "dev": "lerna run dev --stream"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.1.0"
  }
}
